
<div class="top-bar">
  <% if current_user  %>
  <%= "Welcome, #{current_user.uid}" %>
  <%= link_to "Logout", logout_path %>
  <% else %>
  <%= link_to "Sign in with Github", "/auth/github" %>
  <% end %>
</div>
